{% extends 'base.html.twig' %}

{% block title %}VitrineTux{% endblock %}

{% block body %}
    <h1>VitrineTux</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ vitrine_tux.id }}</td>
            </tr>
            <tr>
                <th>Ispublic</th>
                <td>{{ vitrine_tux.ispublic ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ vitrine_tux.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_vitrine_tux_index') }}">back to list</a>

    <a href="{{ path('app_vitrine_tux_edit', {'id': vitrine_tux.id}) }}">edit</a>

    {{ include('vitrine_tux/_delete_form.html.twig') }}
{% endblock %}
