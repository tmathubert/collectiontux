{% extends 'base.html.twig' %}

{% block title %}VitrineTux{% endblock %}

{% block main %}
    <h1>VitrineTux {{vitrine.name}} </h1>

    <h1>Carte {{ carte.description }}</h1>
    <table class="table">
        <tbody>
            <tr>
                <th>ID :</th>
                <td>{{ carte.id }}</td>
              </tr>
            <tr>
                <th>Type :</th>
                <td>{{ carte.type }}</td>
            </tr>
            <tr>
                <th>Prix :</th>
                <td>{{ carte.prix }}</td>
            </tr>
            <tr>
                <a href="{{ path('app_vitrine_tux_show', { 'id': vitrine.id}) }}">back to VitrineTux</a>
            </tr>
        </tbody>
    </table>
{% endblock %}
